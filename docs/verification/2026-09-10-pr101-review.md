# PR 101 review and next implementation checkpoint

PR: https://github.com/Pin4sf/Waldo-Kennel/pull/101
Reviewed head: `b95bc71b947ce9b185b19cb18353f7d7adb7cee5`.
Comparison: `ae7aa5690...b95bc71b9` (Claude's nine commits since the Luna review).
PR targets beta and was open when checked; GitHub returned an empty statusCheckRollup. Local HEAD matched the PR and was clean. No merge, push, PR comment/edit or live-profile mutation was performed.

## Verdict

Useful corrections landed, but this is not ready for approval as a completed Work loop. Continue implementation after resolving the issues below. The absence of materialization is acknowledged correctly, but the partial gate currently allows an unsafe success path; documentation alone does not prevent that launch.

## Standards axis

Manifest-backed review unavailable; current AGENTS.md capability authority rules used. Reviewer: standards_review, independently reproduced by primary reviewer. Revision for both findings is the reviewed head above. Both findings are open, confidence high, severity P1, descriptive rule ID `AGENTS.capability-authority-least-privilege`.

1. **Restricted grants widen.** `backend/internal/governedcheck/enforcement.go:56` checks capability names, not grant scopes. `enforcement_darwin.go:30` converts the presence of worktree.write into a whole-workspace write grant. A temporary regression gave write scope `worktree/docs/*` and executed touch elsewhere in the temporary workspace: the write succeeded with nil error. Reject unsupported scopes or map them exactly before process creation. Preserve the exact supported-capability-set checks already established at provider adapter boundaries.

2. **Data reads are unrestricted, even with no read capability.** `enforcement_darwin.go:70` grants `(allow file-read*)` unconditionally. The existing checkPolicy test helper grants only worktree.exec. An outside-data read is therefore permitted despite absent worktree.read authority. Validate supported capability combinations and distinguish required runtime/library reads from arbitrary owner data reads. Add an outside-data canary and exec-only refusal test; do not label this policy fully conformant merely because writes/network are blocked.

## Spec axis

Source: `docs/superpowers/plans/2026-09-10-luna-kennel-work-completion.md`. Reviewer: spec_review; source findings confirmed by primary review.

1. **P1 — C-13 still launches without dependency content.** `service/outcome/handoff.go:155` resolves receipts and discards them. `attempt.go` then continues through unchanged spawn. Complete frozen metadata permits launch even though blobs were not verified, versions were not frozen into successor admission, and materialization did not happen. Until verified input provisioning is integrated, fail dependent launch closed with an explicit unavailable reason. Complete the approved-base plus retained-output materialization before provider launch, preserving unchanged files, modes, deletions and multi-parent conflict handling. Prove A → daemon restart → B actual bytes at the real launch boundary, not only the pure receipt resolver.

2. **P1 — Timestamp revalidation is not commit ordering.** `storage/sqlite/queries/outcome_proof.sql:83` counts facts by created_at >= proofObservedAt. Evidence/Verification timestamps are assigned in `service/outcome/proof.go` before the storage writer lock. A contradictory writer can receive time t0, wait while reconciliation reads at t1, then commit before classification with created_at < t1. The count misses it and stale proof can succeed. Use a transactionally validated durable generation/fingerprint or evaluate proof within the finalizing transaction. Add a deterministic delayed-writer test; wall-clock precision or another untransactional read does not fix ordering. Include correction/rework facts that alter the proof horizon.

Additional projection issue: `service/outcome/scheduler.go:227` intentionally ignores artifact version. Proof for prior bytes can leave a WorkUnit displayed proven even when current receipt classification is unproved. Align schedule readiness with eligible current artifact proof; a later admission refusal does not make a misleading Ready/Start projection correct.

## Verification

Fresh `-count=1` runs passed:

```sh
cd backend
go test ./internal/governedcheck ./internal/artifactstore ./internal/service/outcome ./internal/storage/sqlite/store ./internal/service/intelligence -count=1
go test ./internal/httpd/... -count=1
```

These include macOS denied-write, denied-network and allowed-workspace-write canaries. They establish meaningful local Seatbelt behavior, not live provider conformance or full capability mapping. New scope/read regression checks used only disposable temporary files; temporary test sources were removed. No real credentials were read.

Earlier byte-bound receipt, filename whitespace, export collision and synchronization-order corrections are present with tests. Donor rename code inspected preserves the legacy managed-gitignore sentinel and historical commit identities. The large removed marketing/docs trees were not individually audited as a product acceptance exercise. Full frontend, full race suite and packaged launch were not repeated in this review.

## PR description and remaining work

The PR body is stale: it says no enforcement exists on any host and SP1 is blocked everywhere, while latest code implements Darwin Seatbelt. It also says every independent finding is closed while listing open SP2/SP4/SP5. Rewrite around the current head: fixes landed, macOS mechanism exists with scope findings, and production integration remains partial. No GitHub checks were visible; local evidence is not a GitHub CI pass.

Next Claude sequence:

1. Fix scope/read authority and proof transaction ordering; add permanent regressions.
2. Finish C-13 verified blobs, immutable input versions, workspace materialization and restart-safe admission. Keep the incomplete dependent path fail-closed while doing this.
3. Wire governed checks, exact artifact-bound evidence and durable run intent/rework. A standalone enforcing runner is not D completion.
4. Finish B1 status board/B3 Mission actions; UI work may proceed independently, but do not call the software loop complete before D.
5. Wire document Outcomes and durable export service/API/UI; complete focused main-process startup and build the focused package.
6. Run the full matrix including real-daemon and packaged journeys, clearly separating live provider and owner acceptance.

Keep commits bounded and update the existing implementation ledger. Do not add more broad cleanup to this checkpoint. Do not use session estimates as proof of readiness. Continue from the existing code instead of restarting the build.

Standards: 2 findings, worst P1 (capability widening). Spec: 2 findings, worst P1 (unsafe successor launch/stale proof), plus one schedule projection issue.
